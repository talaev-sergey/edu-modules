# Домашняя работа

## Урок №1 — Что такое контроль версий. Установка Git и первая настройка

**Срок сдачи:** к уроку 2

### Задание

Задание на повторение (письменно в тетради или в файле):

1. Своими словами объясни, что такое система контроля версий и какую проблему она решает (2–3 предложения).
2. Напиши, в каком году и кто создал Git.
3. Выпиши три команды, которые мы использовали на уроке, и подпиши, что делает каждая (`git --version`, `git config --global user.name`, `git config --list`).
4. Если дома есть компьютер — установи на нём Git и редактор VS Code, затем выполни первую настройку Git (имя и e-mail) самостоятельно.

### Критерии выполнения

- Дано определение системы контроля версий и описана решаемая ею проблема.
- Указаны год создания Git и его автор.
- Выписаны три команды с пояснением, что делает каждая.
- (При наличии домашнего компьютера) Git и VS Code установлены, выполнена первичная настройка Git.

---

## Урок №2 — Первый репозиторий: init, add, commit, status, log

**Срок сдачи:** к уроку 3

### Задание

Задание на повторение (письменно в тетради или в файле):

1. Выпиши по порядку пять команд урока (`git init`, `git status`, `git add`, `git commit`, `git log`) и подпиши, что делает каждая.
2. Своими словами объясни, что такое индекс и зачем нужен `git add` перед коммитом (2–3 предложения).
3. Нарисуй или опиши путь файла из трёх ступеней: рабочая папка, индекс, коммит.
4. Если дома есть компьютер с Git — создай репозиторий `my-home-repo`, добавь в него один файл и сделай первый коммит.

### Критерии выполнения

- Выписаны пять команд урока с пояснением, что делает каждая.
- Объяснено, что такое индекс и зачем нужен `git add` перед коммитом.
- Изображён или описан путь файла из трёх ступеней (рабочая папка → индекс → коммит).
- (При наличии домашнего компьютера) Создан репозиторий `my-home-repo` с первым коммитом.

---

## Урок №3 — История изменений: просмотр, отмена, возврат к версии

**Срок сдачи:** к уроку 4

### Задание

Задание на повторение (письменно в тетради или в файле):

1. Выпиши команды урока (`git log --oneline`, `git show`, `git diff`, `git restore`, `git revert`, `git checkout`) и подпиши, что делает каждая.
2. Своими словами объясни разницу между `git restore`, `git revert` и `git checkout` — по одному предложению на каждую.
3. Нарисуй ленту из трёх коммитов программы, подпиши их короткими хешами и стрелкой покажи, где находится `HEAD`.
4. Если дома есть компьютер с Git и VS Code — создай репозиторий `home-project`, напиши программу из нескольких функций, делая отдельный коммит на каждую, и потренируйся: посмотри историю и сделай временный переход в прошлое — в первый коммит, а потом вернись назад.

### Критерии выполнения

- Выписаны команды урока с пояснением, что делает каждая.
- Объяснена разница между `git restore`, `git revert` и `git checkout`.
- Изображена лента из трёх коммитов с короткими хешами и указателем `HEAD`.
- (При наличии домашнего компьютера) Создан репозиторий `home-project`, сделаны коммиты по функциям, выполнен переход в прошлое и возврат.

---

## Урок №4 — Ветки: создание, переключение, слияние

**Срок сдачи:** к уроку 5

### Задание

Повторение. Возьми свою папку `my-first-app` и потренируй полный цикл работы с веткой ещё раз — на новой маленькой задаче:

1. Создай ветку `feature-hello`.
2. Допиши в `calc.py` функцию `greeting()`, которая возвращает строку `"Привет! Это мой калькулятор"`.
3. Сделай коммит, переключись на основную ветку и убедись, что функции там пока нет.
4. Влей ветку в основную и удали её.

Письменно ответь одной-двумя строками на вопросы (можно в обычном текстовом файле):

- Зачем нужна отдельная ветка, если можно писать код сразу в основной?
- Какой командой узнать, на какой ветке ты сейчас находишься?

### Критерии выполнения

- Создана ветка `feature-hello`, в `calc.py` добавлена функция `greeting()`.
- Сделан коммит, выполнено переключение на основную ветку с проверкой, что функции там пока нет.
- Ветка влита в основную и удалена.
- Письменно дан ответ на оба вопроса (зачем нужна отдельная ветка; какой командой узнать текущую ветку).

---

## Урок №5 — GitHub: аккаунт, удалённый репозиторий, push и pull

**Срок сдачи:** к уроку 6

### Задание

Повторение. Зайди на свою страницу репозитория `my-first-app` на GitHub из дома (или с любого компьютера) и убедись, что проект на месте и доступен по ссылке. Затем потренируй полный круг синхронизации:

1. На компьютере допиши в `calc.py` функцию `subtract_three(a, b, c)`, которая возвращает `a - b - c`.
2. Сделай коммит и отправь его командой `git push`.
3. Проверь на сайте GitHub, что функция появилась.

Письменно ответь одной-двумя строками на вопросы (можно в обычном текстовом файле):

- Чем удалённый репозиторий отличается от локального?
- Какой командой отправляют изменения на GitHub, а какой — забирают их к себе?

### Критерии выполнения

- Репозиторий `my-first-app` открыт на GitHub и доступен по ссылке.
- В `calc.py` добавлена функция `subtract_three(a, b, c)`, сделан коммит и выполнен `git push`.
- На сайте GitHub проверено, что функция появилась.
- Письменно дан ответ на оба вопроса (отличие удалённого репозитория от локального; команды отправки и получения изменений).

---

## Урок №6 — Клонирование и работа с чужими проектами: clone, fork

**Срок сдачи:** к уроку 7

### Задание

Повторение. Дома (или с любого компьютера) потренируй `clone` ещё раз, как будто садишься за новый компьютер:

1. Создай новую пустую папку, открой её в VS Code и в терминале склонируй свой репозиторий `my-first-app` с GitHub командой `git clone <адрес>`.
2. Открой скачанную папку и набери `git log --oneline` — убедись, что приехала вся история коммитов.

Письменно ответь одной-двумя строками на вопросы (можно в обычном текстовом файле):

- Чем `clone` отличается от форка?
- Если ты сделал форк чужого проекта и отправил в свой форк изменение командой `push` — изменится ли при этом оригинальный (чужой) репозиторий? Почему?

### Критерии выполнения

- Создана новая пустая папка, в неё склонирован репозиторий `my-first-app` командой `git clone`.
- В скачанной папке выполнен `git log --oneline` и проверено, что приехала вся история коммитов.
- Письменно дан ответ на оба вопроса (чем `clone` отличается от форка; изменится ли оригинал при `push` в свой форк и почему).

---

## Урок №7 — Командная работа: Pull Request, Issues, обзор кода

**Срок сдачи:** к уроку 8

### Задание

Повторение. Дома закрепи командный цикл и письменно ответь на вопросы.

1. Зайди на GitHub в свой проект `my-first-app`, открой вкладку **Issues** и создай новую Issue с любой полезной идеей для калькулятора, например: `Добавить куб числа` или `Выводить результат с пояснением`. Запиши её номер.
2. Письменно ответь одной-двумя строками на вопросы (можно в обычном текстовом файле):
   - Чем Pull Request отличается от обычного `push` в свой форк?
   - Что должно произойти, чтобы предложенное в Pull Request изменение попало в проект?
   - Каким должен быть хороший комментарий при обзоре кода?

### Критерии выполнения

- В репозитории `my-first-app` создана новая Issue с полезной идеей для калькулятора, записан её номер.
- Письменно дан ответ на вопрос, чем Pull Request отличается от обычного `push` в свой форк.
- Письменно дан ответ на вопрос, что должно произойти, чтобы изменение из Pull Request попало в проект (merge автором).
- Письменно дан ответ на вопрос, каким должен быть хороший комментарий при обзоре кода.

---

## Урок №8 — Итоговый практикум: общий сайт класса на GitHub Pages

**Срок сдачи:** к контрольной работе

### Задание

Повтори темы прошлых уроков для подготовки к контрольной работе на следующем уроке. Пройдись по конспектам уроков 1–7 и письменно (в тетради или в файле) кратко ответь на вопросы:

1. Что такое система контроля версий и какую задачу решает Git (урок 1)?
2. Назови по порядку команды первого репозитория: `init`, `add`, `commit`, `status`, `log` — и что делает каждая (урок 2).
3. Чем отличаются `git restore`, `git revert` и `git checkout` при работе с историей (урок 3)?
4. Зачем нужны ветки и какими командами их создают, переключают и сливают (урок 4)?
5. Чем удалённый репозиторий отличается от локального; какие команды отправляют и забирают изменения (урок 5)?
6. Чем `clone` отличается от форка (урок 6)?
7. Что такое Pull Request, Issue и обзор кода; что происходит при merge (урок 7)?
8. Что такое GitHub Pages и как из репозитория получается опубликованный сайт (урок 8)?

### Критерии выполнения

- Повторены конспекты уроков 1–8.
- Письменно даны краткие ответы на вопросы по всем темам модуля.
- Ученик готов к контрольной работе по модулю.

---
