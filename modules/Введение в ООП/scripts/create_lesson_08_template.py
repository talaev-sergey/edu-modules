from docx import Document
from docx.shared import Pt


OUTPUT = "content/lesson_08.docx"


def add_table(document, headers, rows):
    table = document.add_table(rows=1, cols=len(headers))
    table.style = "Table Grid"
    for index, header in enumerate(headers):
        table.rows[0].cells[index].text = header
    for row in rows:
        cells = table.add_row().cells
        for index, value in enumerate(row):
            cells[index].text = value
    return table


def add_label_paragraph(document, label, text=""):
    paragraph = document.add_paragraph()
    paragraph.add_run(label).bold = True
    if text:
        paragraph.add_run(text)
    return paragraph


def add_code(document, code):
    for line in code.strip("\n").splitlines():
        paragraph = document.add_paragraph()
        run = paragraph.add_run(line)
        run.font.name = "Consolas"
        run.font.size = Pt(10)


def add_numbered(document, items):
    for item in items:
        document.add_paragraph(item, style="List Number")


def add_bullets(document, items):
    for item in items:
        document.add_paragraph(item, style="List Bullet")


doc = Document()
doc.add_heading("Урок 8 — Итоговый проект и подготовка к Unity", level=1)

doc.add_heading("Общая информация", level=2)
add_table(
    doc,
    ["Параметр", "Значение"],
    [
        ["Курс", "Программирование на C#"],
        ["Модуль", "Введение в ООП"],
        ["Номер урока", "8"],
        ["Возраст учащихся", "12-15 лет"],
        ["Продолжительность", "120 мин"],
    ],
)

doc.add_heading("Цель урока", level=2)
doc.add_paragraph(
    "К концу урока ученики смогут доработать RPG-проект: добавить простой выбор действия через `Console.ReadLine()`, "
    "хранить несколько противников в `List<Enemy>`, показать один рабочий сценарий и коротко объяснить, как классы "
    "и объекты из проекта связаны с будущими Unity-объектами."
)

doc.add_heading("План урока", level=2)
add_table(
    doc,
    ["Этап", "Время"],
    [
        ["1. Организационный момент", "5 мин"],
        ["2. Теоретическая часть", "10 мин"],
        ["3. Практическая работа", "65 мин"],
        ["4. Самостоятельная работа", "30 мин"],
        ["5. Подведение итогов", "10 мин"],
        ["Итого", "120 мин"],
    ],
)

doc.add_heading("Ход занятия", level=2)

doc.add_heading("1. Организационный момент (5 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Проверить, что у учеников открыт Visual Studio 2022 и доступен проект с прошлого урока, например `OopLesson7`.",
        "Попросить открыть файл `Program.cs` через `Solution Explorer`. Если окна нет, открыть его через меню `View` -> `Solution Explorer`.",
        "Проверить, что проект запускается кнопкой запуска Visual Studio или через `dotnet run`. Если проект не запускается, сначала исправить первую ошибку из списка ошибок Visual Studio.",
        "Сказать цель урока коротко: сегодня проект превращается в итоговую версию с выбором действия, списком противников и демонстрацией результата.",
        "Подчеркнуть ограничение: мы не делаем большую игру, а собираем понятный учебный проект, где видно, как работают классы, объекты, методы и списки.",
    ],
)

doc.add_heading("2. Теоретическая часть (10 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Урок проводится в режиме B: вводная теория короткая, а основные объяснения встроены в практику. На экране показать схему: "
    "`Program.cs` задает сценарий, но действия выполняют объекты `Hero`, `Enemy` и `Item`. Затем показать, что в Unity идея похожая: "
    "объект на сцене тоже имеет данные и действия, но сегодня мы остаемся в консольном C#."
)

doc.add_heading("Итоговая сборка проекта", level=4)
doc.add_paragraph(
    "Итоговая сборка не означает, что нужно добавить как можно больше кода. Хороший финальный проект запускается, понятен со стороны "
    "и показывает несколько изученных идей: классы, объекты, конструкторы, методы, защищенные данные и список объектов. Если проект "
    "становится слишком большим, его сложнее демонстрировать и исправлять."
)

doc.add_heading("Выбор действия", level=4)
doc.add_paragraph(
    "`Console.ReadLine()` позволяет получить строку, которую ввел пользователь. Для простого меню достаточно сравнить эту строку "
    "с вариантами `\"1\"`, `\"2\"`, `\"3\"`. Важно: `Program.cs` только спрашивает выбор и вызывает методы, а логика атаки, получения урона "
    "и инвентаря остается внутри классов."
)

doc.add_heading("Связь с Unity", level=4)
doc.add_paragraph(
    "В Unity ученики позже встретят объекты на сцене, у которых есть данные и поведение. Сегодняшний `Hero` похож на описание героя, "
    "`Enemy` - на описание противника, а методы вроде `Attack()` и `TakeDamage()` похожи на действия, которые объект умеет выполнять. "
    "Не нужно подробно разбирать Unity: достаточно связать идею класса и объекта с будущими сценами."
)

doc.add_heading("Записи в блокнот", level=4)
for term, definition in [
    ("Итоговая сборка проекта", "доработка проекта до состояния, когда он запускается, демонстрирует основную идею и понятен зрителю."),
    ("`Console.ReadLine()`", "команда, которая читает строку, введенную пользователем в консоли."),
    ("Выбор действия", "простое меню, где пользователь вводит номер, а программа выполняет соответствующий блок кода."),
    ("`List<Enemy>`", "список объектов класса `Enemy`, в котором можно хранить несколько противников."),
    ("Демонстрация проекта", "короткий показ: запуск, выбор действия, видимый результат и объяснение, какие классы участвуют."),
    ("Связь с Unity", "понимание, что объект в игре тоже хранит данные и умеет выполнять действия."),
]:
    paragraph = doc.add_paragraph()
    paragraph.add_run(term).bold = True
    paragraph.add_run(" — " + definition)

doc.add_heading("3. Практическая работа (65 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Вести практику по цепочке `показываю -> делаем вместе -> ученик дорабатывает сам`. Сначала вместе проверить проект прошлого урока, "
    "затем добавить список противников, меню выбора и финальную демонстрацию. После каждого мини-блока запускать проект, чтобы ошибки "
    "не накопились к концу занятия."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Мини-теория 1. Проверяем основу проекта и фиксируем план финальной версии.")
doc.add_paragraph(
    "Перед добавлением нового кода ученики должны увидеть, что проект прошлого урока уже содержит базовые классы. Если у ученика проект "
    "не готов, он может взять минимальный каркас преподавателя и доработать его. Главное решение урока: финальная версия должна быть простой, "
    "но полностью запускаемой."
)
add_numbered(
    doc,
    [
        "Открыть Visual Studio 2022.",
        "Открыть проект прошлого урока через `File` -> `Open` -> `Project/Solution` или выбрать его на стартовом экране Visual Studio.",
        "В `Solution Explorer` открыть `Program.cs`.",
        "Запустить проект и убедиться, что в консоли появляется сцена из прошлого урока.",
        "В начале `Program.cs` записать комментарий с планом итогового проекта.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — Program.cs с открытым проектом прошлого урока и комментарием-планом итоговой версии]")
add_code(
    doc,
    '''
// План итогового проекта:
// 1. Создать героя.
// 2. Создать несколько противников и положить их в список.
// 3. Показать игроку возможные действия.
// 4. По выбору игрока вызвать метод нужного объекта.
// 5. После действия показать результат.
''',
)

doc.add_paragraph("Мини-теория 2. Храним нескольких противников в `List<Enemy>`.")
doc.add_paragraph(
    "Список нужен, когда объектов одного типа становится несколько. Вместо трех отдельных переменных можно создать `List<Enemy>` и добавить "
    "в него противников через `Add()`. Для вывода всех противников используется `foreach`: он по очереди берет каждый объект из списка."
)
add_numbered(
    doc,
    [
        "В верхней части `Program.cs` проверить строку `using System.Collections.Generic;`.",
        "Создать героя и предмет-награду.",
        "Создать `List<Enemy>`.",
        "Добавить в список минимум трех противников через `Add()`.",
        "Вывести список противников через `foreach` и метод `ShowInfo()`.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — код со строкой using System.Collections.Generic, созданием List<Enemy> и тремя вызовами Add()]")
add_code(
    doc,
    '''
using System.Collections.Generic;

Hero hero = new Hero("Рыцарь", 100, 25);
Item reward = new Item("Зелье лечения", "Восстанавливает здоровье героя.");

List<Enemy> enemies = new List<Enemy>();
enemies.Add(new Enemy("Тренировочный противник", 25, 8));
enemies.Add(new Enemy("Быстрый противник", 40, 12));
enemies.Add(new Enemy("Тяжелый противник", 90, 15));

Console.WriteLine("Противники на уровне:");
foreach (Enemy enemy in enemies)
{
    enemy.ShowInfo();
}
''',
)

doc.add_paragraph("Мини-теория 3. Добавляем меню выбора действия.")
doc.add_paragraph(
    "`Console.ReadLine()` возвращает строку. Поэтому выбор сравнивается не с числом `1`, а со строкой `\"1\"`. На этом этапе не нужно делать "
    "сложный ввод номера противника: достаточно выбрать действие и атаковать первого противника из списка через `enemies[0]`."
)
add_numbered(
    doc,
    [
        "После вывода списка создать переменную `Enemy currentEnemy = enemies[0];`.",
        "Вывести три варианта действия в консоль.",
        "Считать выбор пользователя в переменную `choice`.",
        "Добавить цепочку `if`, `else if`, `else`.",
        "Внутри вариантов вызывать методы объектов: `hero.Attack(currentEnemy)`, `hero.ShowInfo()`, `enemy.ShowInfo()`.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — меню действий, Console.ReadLine() и блок if/else с выбором по строкам \"1\", \"2\", \"3\"]")
add_code(
    doc,
    '''
Enemy currentEnemy = enemies[0];

Console.WriteLine();
Console.WriteLine("Выберите действие:");
Console.WriteLine("1 - атаковать первого противника");
Console.WriteLine("2 - посмотреть состояние героя");
Console.WriteLine("3 - посмотреть список противников");

string choice = Console.ReadLine();

if (choice == "1")
{
    hero.Attack(currentEnemy);
    currentEnemy.ShowInfo();
}
else if (choice == "2")
{
    hero.ShowInfo();
}
else if (choice == "3")
{
    foreach (Enemy enemy in enemies)
    {
        enemy.ShowInfo();
    }
}
else
{
    Console.WriteLine("Такого действия пока нет.");
}
''',
)

doc.add_paragraph("Мини-теория 4. Собираем финальную версию и готовим демонстрацию.")
doc.add_paragraph(
    "Финальная демонстрация должна быть короткой: запустить проект, выбрать действие, показать результат, объяснить классы. Если первый "
    "противник побежден, герой получает предмет через `AddItem()`, а инвентарь выводится через `ShowInventory()`. Это показывает, что "
    "объекты не просто хранят данные, а взаимодействуют через методы."
)
add_numbered(
    doc,
    [
        "Добавить проверку `currentEnemy.IsDefeated()` после атаки.",
        "Если противник побежден, вывести сообщение и добавить награду через `hero.AddItem(reward)`.",
        "После блока выбора вывести инвентарь героя через `hero.ShowInventory()`.",
        "Запустить проект и проверить вариант `1`.",
        "Запустить проект второй раз и проверить вариант `2` или `3`.",
        "Подготовить короткое объяснение: какие классы есть в проекте и что делает каждый класс.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: консоль — пользователь выбрал действие 1, герой атаковал, противник получил урон, награда добавлена в инвентарь]")
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 и консоль — финальная версия проекта готова к демонстрации]")
doc.add_paragraph("Полный пример итоговой версии:")
add_code(
    doc,
    '''
using System.Collections.Generic;

Hero hero = new Hero("Рыцарь", 100, 25);
Item reward = new Item("Зелье лечения", "Восстанавливает здоровье героя.");

List<Enemy> enemies = new List<Enemy>();
enemies.Add(new Enemy("Тренировочный противник", 25, 8));
enemies.Add(new Enemy("Быстрый противник", 40, 12));
enemies.Add(new Enemy("Тяжелый противник", 90, 15));

Console.WriteLine("Итоговая RPG-сцена");
Console.WriteLine("Противники на уровне:");

foreach (Enemy enemy in enemies)
{
    enemy.ShowInfo();
}

Enemy currentEnemy = enemies[0];

Console.WriteLine();
Console.WriteLine("Выберите действие:");
Console.WriteLine("1 - атаковать первого противника");
Console.WriteLine("2 - посмотреть состояние героя");
Console.WriteLine("3 - посмотреть список противников");

string choice = Console.ReadLine();

if (choice == "1")
{
    hero.Attack(currentEnemy);
    currentEnemy.ShowInfo();

    if (currentEnemy.IsDefeated())
    {
        Console.WriteLine("Противник побежден.");
        hero.AddItem(reward);
    }
}
else if (choice == "2")
{
    hero.ShowInfo();
}
else if (choice == "3")
{
    foreach (Enemy enemy in enemies)
    {
        enemy.ShowInfo();
    }
}
else
{
    Console.WriteLine("Такого действия пока нет.");
}

Console.WriteLine();
hero.ShowInventory();

class Item
{
    public string Name { get; private set; }
    public string Description { get; private set; }

    public Item(string name, string description)
    {
        Name = name;
        Description = description;
    }

    public void ShowInfo()
    {
        Console.WriteLine("Предмет: " + Name);
        Console.WriteLine(Description);
    }
}

class Enemy
{
    public string Name { get; private set; }
    public int Health { get; private set; }
    public int Damage { get; private set; }

    public Enemy(string name, int health, int damage)
    {
        Name = name;
        Health = health;
        Damage = damage;
    }

    public void TakeDamage(int damage)
    {
        Health = Health - damage;

        if (Health < 0)
        {
            Health = 0;
        }

        Console.WriteLine(Name + " получает " + damage + " урона.");
    }

    public bool IsDefeated()
    {
        return Health == 0;
    }

    public void ShowInfo()
    {
        Console.WriteLine(Name + ": здоровье " + Health + ", урон " + Damage);
    }
}

class Hero
{
    public string Name { get; private set; }
    public int Health { get; private set; }
    public int Damage { get; private set; }

    private List<Item> _items = new List<Item>();

    public Hero(string name, int health, int damage)
    {
        Name = name;
        Health = health;
        Damage = damage;
    }

    public void ShowInfo()
    {
        Console.WriteLine(Name + ": здоровье " + Health + ", урон " + Damage);
    }

    public void Attack(Enemy enemy)
    {
        Console.WriteLine(Name + " атакует: " + enemy.Name);
        enemy.TakeDamage(Damage);
    }

    public void AddItem(Item item)
    {
        _items.Add(item);
        Console.WriteLine(Name + " получает предмет: " + item.Name);
    }

    public void ShowInventory()
    {
        Console.WriteLine("Инвентарь героя " + Name + ":");

        if (_items.Count == 0)
        {
            Console.WriteLine("Пока пусто.");
            return;
        }

        foreach (Item item in _items)
        {
            Console.WriteLine("- " + item.Name);
        }
    }
}
''',
)
add_label_paragraph(
    doc,
    "Ожидаемый результат:",
    " у ученика есть итоговый консольный проект на C#, где несколько противников хранятся в `List<Enemy>`, пользователь выбирает действие через `Console.ReadLine()`, а основные действия выполняются методами объектов.",
)

doc.add_heading("4. Самостоятельная работа (30 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Дать ученикам 30 минут на доработку своей версии. В первые 5 минут попросить не добавлять новые большие идеи, а выбрать один понятный "
    "план завершения. Затем обходить класс по контрольному списку: проект запускается, меню работает, список противников есть, ученик может "
    "объяснить классы. Если ученик застрял, давать подсказку следующего уровня, а не готовый код сразу."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Заверши свою итоговую версию RPG-проекта и подготовь ее к короткой демонстрации.")
doc.add_paragraph("Минимальные требования:")
add_numbered(
    doc,
    [
        "Проект запускается без ошибок.",
        "В проекте есть классы `Hero`, `Enemy`, `Item` или похожие по смыслу классы.",
        "В коде есть `List<Enemy>` минимум с двумя противниками.",
        "В консоли появляется меню действий.",
        "Выбор пользователя считывается через `Console.ReadLine()`.",
        "Хотя бы одно действие вызывает метод объекта, например `Attack()`, `ShowInfo()` или `ShowInventory()`.",
        "После запуска видно, что действие изменило или показало состояние проекта.",
        "Ученик может за 30-60 секунд объяснить, как проект связан с идеей Unity-объектов.",
    ],
)
doc.add_paragraph("Варианты доработки на выбор:")
add_bullets(
    doc,
    [
        "Добавить действие `4 - показать инвентарь`.",
        "Добавить второго предмета и выдавать его после победы.",
        "Добавить действие `атаковать второго противника` без сложного ввода номера.",
        "Добавить метод `ShowProjectSummary()`, который выводит короткое описание проекта.",
        "Изменить тексты вывода так, чтобы сцена выглядела как законченная история.",
    ],
)

doc.add_heading("Критерии оценки", level=4)
add_table(
    doc,
    ["Результат", "Оценка"],
    [
        ["Проект запускается; есть меню через `Console.ReadLine()`, `List<Enemy>`, минимум три класса, рабочее действие, инвентарь или состояние, а ученик уверенно объясняет связь с Unity", "Отлично"],
        ["Проект запускается и основные требования выполнены, но есть мелкие недочеты в выводе, названиях, количестве вариантов меню или объяснении", "Хорошо"],
        ["Проект работает частично; есть классы и часть меню, но список противников или выбранное действие потребовали помощи преподавателя", "Удовлетворительно"],
        ["Проект не запускается или в нем нет понятной связи между классами, объектами и действиями", "Требует доработки"],
    ],
)

doc.add_heading("5. Подведение итогов (10 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Попросить 2-3 учеников показать итоговый проект: запуск, выбор действия, результат в консоли.",
        "После каждой демонстрации задать один вопрос: какой класс за что отвечает?",
        "Вернуться к идее инкапсуляции: здоровье противника меняется через `TakeDamage()`, а не напрямую из `Program.cs`.",
        "Вернуться к идее списка: несколько противников удобно хранить в `List<Enemy>`, а не в отдельных случайных переменных.",
        "Коротко связать проект с Unity: в будущих сценах объекты тоже будут иметь данные и методы, только результат будет виден не в консоли, а на сцене.",
        "Зафиксировать, что модуль завершен: ученики прошли путь от первого класса до небольшого проекта.",
    ],
)
add_label_paragraph(doc, "Вопросы для рефлексии:")
add_bullets(
    doc,
    [
        "Что в итоговом проекте отвечает за сценарий: `Program.cs` или классы?",
        "Почему выбор из консоли мы сравниваем со строкой `\"1\"`, а не с числом `1`?",
        "Зачем нужен `List<Enemy>`, если можно создать отдельные переменные?",
        "Какой метод защищает здоровье противника от неправильного изменения?",
        "Какой класс в твоем проекте больше всего похож на будущий объект в Unity?",
        "Что бы ты улучшил в проекте, если бы было еще одно занятие?",
    ],
)

doc.add_heading("Домашнее задание", level=2)
doc.add_paragraph("Вариант без компьютера:")
add_numbered(
    doc,
    [
        "Придумать три объекта для будущей Unity-сцены.",
        "Для каждого объекта записать 2-3 данных и 2 действия.",
        "Отметить, какой объект можно было бы описать классом, а какие конкретные экземпляры создать в сцене.",
    ],
)
doc.add_paragraph("Вариант с компьютером:")
add_numbered(
    doc,
    [
        "Открыть итоговый проект.",
        "Добавить один новый предмет, противника или действие меню.",
        "Проверить, что проект запускается без ошибок.",
        "Подготовить объяснение из трех предложений: какие классы есть в проекте, как они взаимодействуют и чем это похоже на будущую работу в Unity.",
    ],
)

doc.add_heading("Методические заметки преподавателя", level=2)

doc.add_heading("Возможные сложности", level=3)
add_bullets(
    doc,
    [
        "Ученики могут пытаться расширять проект слишком сильно: вводить сложный выбор противника, циклы меню и новые правила боя. Ограничивайте объем до одной рабочей демонстрации.",
        "В `Console.ReadLine()` ученики часто забывают, что результат является строкой, и сравнивают `choice == 1` вместо `choice == \"1\"`.",
        "При работе со списком часто забывают `using System.Collections.Generic;` или строку `new List<Enemy>()`.",
        "При обращении `enemies[0]` может возникнуть ошибка, если список пуст. Проверяйте, что вызовы `Add()` стоят до обращения к первому элементу.",
        "В блоках `if/else` ученики часто теряют фигурные скобки. Если ошибок много, сначала найти границы одного блока, а затем переходить к следующему.",
        "Некоторые ученики будут менять `Health` напрямую из `Program.cs`. Возвращайте их к идее: состояние объекта меняется через метод самого объекта.",
        "Слабые навыки Windows могут проявиться в том, что ученик открыл отдельный файл, а не проект Visual Studio. Проверяйте наличие `Solution Explorer`, имени проекта и файла `Program.cs` внутри решения.",
        "Unity может увлечь группу в сторону новой темы. Держите объяснение коротким: сегодня только связь идей, без подробного разбора редактора Unity.",
    ],
)

doc.add_heading("Способы помощи", level=3)
add_bullets(
    doc,
    [
        "Если ученик не понимает меню, спросить: какое действие должно выполниться при выборе `1`, и какой метод для этого уже есть?",
        "Если выбор не работает, попросить временно вывести `choice` в консоль и проверить, что сравнение идет со строками `\"1\"`, `\"2\"`, `\"3\"`.",
        "Если список не работает, проверить три строки: создание `List<Enemy>`, вызовы `Add()`, цикл `foreach`.",
        "Если проект не запускается, исправлять первую ошибку Visual Studio сверху вниз, а не весь список сразу.",
        "Если ученик потерял структуру проекта, дать карточки ролей: `Hero` хранит данные героя и его действия; `Enemy` хранит состояние противника; `Item` хранит данные предмета; `Program.cs` запускает сценарий.",
        "Если ученик просит готовое решение, дать только следующий шаг: например, каркас `if (choice == \"1\") { }` и вопрос, какой метод нужно вызвать внутри.",
        "Если ученик сделал минимальный проект раньше остальных, попросить сначала подготовить демонстрацию, а уже потом брать дополнительное задание.",
    ],
)

doc.add_heading("Дополнительные задания для тех, кто справился раньше", level=3)
add_bullets(
    doc,
    [
        "Добавить действие `4 - показать инвентарь` и вызвать `hero.ShowInventory()` только при этом выборе.",
        "Добавить действие `5 - атаковать второго противника`, используя `enemies[1]`.",
        "Добавить метод `ShowProjectSummary()`, который выводит классы проекта и их роли.",
        "Добавить второго предмета и выдавать разные награды за разных противников.",
        "Разнести классы по отдельным файлам через `Project` -> `Add Class`, если ученик уверенно работает с файлами Visual Studio.",
    ],
)

doc.save(OUTPUT)
print(OUTPUT)
