from docx import Document
from docx.shared import Pt


OUTPUT = "content/lesson_03.docx"


def add_table(document, headers, rows):
    table = document.add_table(rows=1, cols=len(headers))
    table.style = "Table Grid"
    for index, header in enumerate(headers):
        table.rows[0].cells[index].text = header
    for row in rows:
        cells = table.add_row().cells
        for index, value in enumerate(row):
            cells[index].text = value
    return table


def add_label_paragraph(document, label, text=""):
    paragraph = document.add_paragraph()
    paragraph.add_run(label).bold = True
    if text:
        paragraph.add_run(text)
    return paragraph


def add_code(document, code):
    for line in code.strip("\n").splitlines():
        paragraph = document.add_paragraph()
        run = paragraph.add_run(line)
        run.font.name = "Consolas"
        run.font.size = Pt(10)


def add_numbered(document, items):
    for item in items:
        document.add_paragraph(item, style="List Number")


def add_bullets(document, items):
    for item in items:
        document.add_paragraph(item, style="List Bullet")


doc = Document()
doc.add_heading("Урок 3 — Конструктор: создаем объект сразу с нужными данными", level=1)

doc.add_heading("Общая информация", level=2)
add_table(
    doc,
    ["Параметр", "Значение"],
    [
        ["Курс", "Программирование на C#"],
        ["Модуль", "Введение в ООП"],
        ["Номер урока", "3"],
        ["Возраст учащихся", "12-15 лет"],
        ["Продолжительность", "120 мин"],
    ],
)

doc.add_heading("Цель урока", level=2)
doc.add_paragraph(
    "К концу урока ученики смогут написать конструктор с параметрами, создать несколько объектов через "
    "`new Hero(...)` и объяснить, зачем нужен `this`."
)

doc.add_heading("План урока", level=2)
add_table(
    doc,
    ["Этап", "Время"],
    [
        ["1. Организационный момент", "5 мин"],
        ["2. Теоретическая часть", "25 мин"],
        ["3. Практическая работа", "50 мин"],
        ["4. Самостоятельная работа", "30 мин"],
        ["5. Подведение итогов", "10 мин"],
        ["Итого", "120 мин"],
    ],
)

doc.add_heading("Ход занятия", level=2)

doc.add_heading("1. Организационный момент (5 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Проверить, что у учеников открыт Visual Studio 2022 и проект прошлого урока или стартовый проект для этого занятия.",
        "Попросить открыть файл `Program.cs` через `Solution Explorer`. Если окно не видно, открыть его через меню `View` -> `Solution Explorer`.",
        "Коротко повторить: поле хранит данные объекта, метод выполняет действие объекта, точка обращается к конкретному объекту.",
        "Показать проблему урока: если объект создается пустым, программист может забыть заполнить важные поля.",
        "Назвать результат занятия: сегодня объект будет создаваться сразу с именем, здоровьем и уроном.",
    ],
)

doc.add_heading("2. Теоретическая часть (25 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Вести объяснение от знакомого кода прошлого урока. Сначала показать длинное ручное заполнение полей после `new Hero()`, "
    "затем задать вопрос: что случится, если одну строку забыть? После этого ввести конструктор как способ создать объект уже "
    "в правильном начальном состоянии."
)

doc.add_heading("Проблема пустого объекта", level=4)
doc.add_paragraph(
    "Когда мы пишем `new Hero()`, появляется объект, но его поля еще могут быть пустыми или равными нулю. "
    "Например, имя может быть не задано, а здоровье и урон останутся `0`. В игре такой герой уже существует, "
    "но он создан неправильно: он не готов к использованию."
)
doc.add_paragraph(
    "Жизненный пример: если собрать карточку персонажа и забыть вписать здоровье, потом непонятно, может ли он участвовать в бою. "
    "Конструктор помогает не забыть важные данные при создании."
)

doc.add_heading("Конструктор", level=4)
doc.add_paragraph(
    "Конструктор — это специальный метод класса, который вызывается при создании объекта через `new`. "
    "Он называется так же, как класс, и не пишет тип возвращаемого значения, даже `void`. Его задача — подготовить новый объект к работе."
)
doc.add_paragraph(
    "Если класс называется `Hero`, то конструктор тоже называется `Hero`. Внутри конструктора можно сразу записать имя, здоровье и урон в поля объекта."
)

doc.add_heading("Параметры конструктора", level=4)
doc.add_paragraph(
    "Параметры конструктора — это данные, которые мы передаем в момент создания объекта. "
    "Запись `new Hero(\"Рыцарь\", 100, 15)` означает: создай героя и сразу передай ему имя, здоровье и урон. "
    "Порядок аргументов должен совпадать с порядком параметров в конструкторе."
)
doc.add_paragraph(
    "Это похоже на анкету: если в анкете сначала имя, потом возраст, потом город, то ответы нужно вписывать в таком же порядке."
)

doc.add_heading("this", level=4)
doc.add_paragraph(
    "`this` означает этот конкретный объект, который сейчас создается или работает. "
    "Оно особенно полезно, когда поле и параметр называются похоже: `this.Name = name`. "
    "Левая часть говорит про поле объекта, правая — про значение, которое пришло в параметре."
)
doc.add_paragraph(
    "На первом знакомстве важно не требовать от учеников сложных правил. Достаточно закрепить простую мысль: `this` помогает явно показать, "
    "что мы обращаемся к полю текущего объекта."
)

doc.add_heading("Записи в блокнот", level=4)
for term, definition in [
    ("Конструктор", "специальный метод, который вызывается при создании объекта через `new`."),
    ("Параметр", "переменная в скобках конструктора или метода, через которую внутрь передают значение."),
    ("`new Hero(...)`", "создание нового объекта класса `Hero` с передачей данных в конструктор."),
    ("`this`", "ссылка на текущий объект; помогает отличить поле объекта от параметра."),
    ("Начальное состояние", "значения полей, с которыми объект появляется после создания."),
    ("Пустой объект", "объект, у которого важные поля еще не заполнены."),
]:
    paragraph = doc.add_paragraph()
    paragraph.add_run(term).bold = True
    paragraph.add_run(" — " + definition)

doc.add_heading("3. Практическая работа (50 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Практику вести через постепенное снятие опоры. Сначала преподаватель полностью показывает проблему пустого объекта и решение через "
    "конструктор. Затем группа вместе переписывает создание нескольких героев. В финальной части ученики самостоятельно меняют значения "
    "и добавляют еще один объект по образцу."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Часть 1. Увидеть проблему пустого объекта.")
add_numbered(
    doc,
    [
        "Открыть Visual Studio 2022.",
        "Открыть учебный проект или создать новый проект `Console App` с именем `OopLesson3`.",
        "Открыть файл `Program.cs`.",
        "Создать класс `Hero` с полями `Name`, `Health`, `Damage` и методом `ShowInfo()`.",
        "Создать объект через `new Hero()` и заполнить только поле `Name`.",
        "Запустить программу и посмотреть, какие значения выводятся для незаполненных полей.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — Program.cs с созданием Hero через new Hero() и отдельным заполнением поля Name]")
doc.add_paragraph("Код для демонстрации проблемы:")
add_code(
    doc,
    '''
Hero hero = new Hero();
hero.Name = "Рыцарь";

hero.ShowInfo();

class Hero
{
    public string Name;
    public int Health;
    public int Damage;

    public void ShowInfo()
    {
        Console.WriteLine("Герой: " + Name);
        Console.WriteLine("Здоровье: " + Health);
        Console.WriteLine("Урон: " + Damage);
    }
}
''',
)
doc.add_paragraph("[СКРИНШОТ: консоль — у героя есть имя, но здоровье и урон выводятся как 0]")

doc.add_paragraph("Часть 2. Добавить конструктор.")
add_numbered(
    doc,
    [
        "Внутри класса `Hero` добавить конструктор `public Hero(string name, int health, int damage)`.",
        "В теле конструктора записать параметры в поля: `Name = name`, `Health = health`, `Damage = damage`.",
        "В основном коде заменить `new Hero()` на `new Hero(\"Рыцарь\", 100, 15)`.",
        "Убрать отдельные строки ручного заполнения полей.",
        "Запустить программу и проверить, что герой сразу создается с нужными данными.",
    ],
)
doc.add_paragraph("Код после добавления конструктора:")
add_code(
    doc,
    '''
Hero knight = new Hero("Рыцарь", 100, 15);
knight.ShowInfo();

class Hero
{
    public string Name;
    public int Health;
    public int Damage;

    public Hero(string name, int health, int damage)
    {
        Name = name;
        Health = health;
        Damage = damage;
    }

    public void ShowInfo()
    {
        Console.WriteLine("Герой: " + Name);
        Console.WriteLine("Здоровье: " + Health);
        Console.WriteLine("Урон: " + Damage);
    }
}
''',
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — конструктор Hero(string name, int health, int damage) и вызов new Hero(\"Рыцарь\", 100, 15)]")

doc.add_paragraph("Часть 3. Добавить `this` и несколько объектов.")
add_numbered(
    doc,
    [
        "Переписать присваивания в конструкторе через `this`: `this.Name = name` и так далее.",
        "Создать объект `mage` через тот же конструктор, но с другими значениями.",
        "Создать объект `archer` через тот же конструктор, но с другими значениями.",
        "Вызвать `ShowInfo()` у каждого объекта.",
        "Сравнить вывод: класс один, но объекты получили разные начальные данные.",
    ],
)
doc.add_paragraph("Код с `this` и несколькими объектами:")
add_code(
    doc,
    '''
Hero knight = new Hero("Рыцарь", 100, 15);
Hero mage = new Hero("Маг", 60, 30);
Hero archer = new Hero("Лучник", 80, 20);

knight.ShowInfo();
mage.ShowInfo();
archer.ShowInfo();

class Hero
{
    public string Name;
    public int Health;
    public int Damage;

    public Hero(string name, int health, int damage)
    {
        this.Name = name;
        this.Health = health;
        this.Damage = damage;
    }

    public void ShowInfo()
    {
        Console.WriteLine("Герой: " + Name);
        Console.WriteLine("Здоровье: " + Health);
        Console.WriteLine("Урон: " + Damage);
        Console.WriteLine();
    }
}
''',
)
doc.add_paragraph("[СКРИНШОТ: консоль — выведены Рыцарь, Маг и Лучник с разными значениями здоровья и урона]")
add_label_paragraph(
    doc,
    "Ожидаемый результат: ",
    "ученик создал класс `Hero`, добавил конструктор с тремя параметрами, создал минимум три объекта через `new Hero(...)` "
    "и увидел, что каждый объект получил собственные начальные значения.",
)

doc.add_heading("4. Самостоятельная работа (30 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Первые 5 минут не давать готовый код, а просить учеников найти в примере `Hero` такие же части: поля, конструктор, создание объекта. "
    "На 10-й минуте сделать быструю проверку: у каждого должен быть класс, три поля и строка конструктора. Если часть группы застряла, "
    "дать каркас класса без готовых значений."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Выбери одну тему и создай свой класс с конструктором по аналогии с `Hero`.")
add_bullets(
    doc,
    [
        "Вариант 1: `Enemy` — поля `Name`, `Health`, `Damage`.",
        "Вариант 2: `Robot` — поля `Model`, `Battery`, `Power`.",
        "Вариант 3: `Item` — поля `Name`, `Price`, `Rarity`.",
        "Вариант 4: `Weapon` — поля `Name`, `Damage`, `Durability`.",
    ],
)
doc.add_paragraph("Минимальные требования:")
add_numbered(
    doc,
    [
        "Создать класс по выбранной теме.",
        "Добавить три поля.",
        "Добавить конструктор с тремя параметрами.",
        "В конструкторе записать параметры в поля. Можно использовать `this`.",
        "Добавить метод `ShowInfo()`.",
        "Создать минимум два объекта через конструктор с разными значениями.",
        "Запустить программу и проверить вывод в консоли.",
    ],
)
doc.add_paragraph("Каркас для тех, кому нужна опора:")
add_code(
    doc,
    '''
class Enemy
{
    public string Name;
    public int Health;
    public int Damage;

    public Enemy(string name, int health, int damage)
    {
        this.Name = name;
        this.Health = health;
        this.Damage = damage;
    }

    public void ShowInfo()
    {
        Console.WriteLine("Имя: " + Name);
        Console.WriteLine("Здоровье: " + Health);
        Console.WriteLine("Урон: " + Damage);
    }
}
''',
)

doc.add_heading("Критерии оценки", level=4)
add_table(
    doc,
    ["Результат", "Оценка"],
    [
        ["Класс, 3 поля, конструктор с 3 параметрами, метод ShowInfo() и 2 объекта созданы; программа запускается", "Отлично"],
        ["Основная задача выполнена, но есть мелкие ошибки в названиях, порядке параметров или оформлении вывода", "Хорошо"],
        ["Класс или конструктор сделан частично; программа заработала после помощи преподавателя", "Удовлетворительно"],
        ["Программа не запускается или конструктор отсутствует", "Требует доработки"],
    ],
)

doc.add_heading("5. Подведение итогов (10 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Попросить 2-3 учеников показать строку конструктора и строку создания объекта через `new ...(...)`.",
        "Задать группе вопрос: почему после появления конструктора мы больше не создаем героя через пустой `new Hero()`?",
        "Закрепить различие: конструктор описывается внутри класса, а вызывается в момент создания объекта.",
        "Показать на одном примере, как порядок аргументов связан с порядком параметров.",
        "Анонсировать следующий урок: данные объекта начнут защищаться через `private` и свойства.",
    ],
)
add_label_paragraph(doc, "Вопросы для рефлексии:")
add_bullets(
    doc,
    [
        "Что делает конструктор?",
        "Почему у конструктора такое же имя, как у класса?",
        "Что передается в скобках при `new Hero(\"Рыцарь\", 100, 15)`?",
        "Зачем может пригодиться `this`?",
        "Какая ошибка появится, если после добавления конструктора написать `new Hero()` без аргументов?",
    ],
)

doc.add_heading("Домашнее задание", level=2)
doc.add_paragraph("Вариант без компьютера:")
add_numbered(
    doc,
    [
        "Придумать класс для игры или учебной программы: `Enemy`, `Robot`, `Item`, `Weapon` или свой вариант.",
        "Записать три поля этого класса.",
        "Записать конструктор с тремя параметрами в виде кода или псевдокода.",
        "Записать два примера создания объектов через `new ClassName(...)`.",
    ],
)
doc.add_paragraph("Вариант с компьютером:")
add_numbered(
    doc,
    [
        "Открыть проект `OopLesson3`.",
        "Создать свой класс с тремя полями, конструктором и методом `ShowInfo()`.",
        "Создать минимум два объекта через конструктор.",
        "Запустить программу и проверить вывод.",
    ],
)

doc.add_heading("Методические заметки преподавателя", level=2)
doc.add_heading("Возможные сложности", level=3)
add_bullets(
    doc,
    [
        "Ученики могут написать у конструктора другой идентификатор. Напомните: имя конструктора полностью совпадает с именем класса.",
        "Ученики могут добавить `void` перед конструктором. Покажите, что тогда это уже обычный метод, а не конструктор.",
        "После добавления конструктора ученики могут продолжать писать `new Hero()` и получать ошибку. Объясните: теперь при создании нужны аргументы.",
        "Порядок аргументов часто путается: `new Hero(100, \"Рыцарь\", 15)` не подходит к параметрам `string, int, int`.",
        "Некоторые ученики воспринимают `this.Name = name` как две одинаковые переменные. Проговаривайте: слева поле объекта, справа параметр.",
        "Могут потеряться фигурные скобки класса или конструктора. Просите сверять пары `{` и `}` и пользоваться отступами Visual Studio.",
        "Слабые навыки Windows проявятся в том, что ученик откроет отдельный файл вместо решения. Проверяйте, что в `Solution Explorer` виден проект и файл `Program.cs`.",
    ],
)

doc.add_heading("Способы помощи", level=3)
add_bullets(
    doc,
    [
        "Если ученик не понимает, зачем конструктор, спросите: где объект получает здоровье и урон в момент создания?",
        "Если ученик путает параметры, попросите прочитать строку конструктора слева направо и подписать: имя, здоровье, урон.",
        "Если ученик просит готовое решение, дайте только начало строки: `public Enemy(string name, int health, int damage)`.",
        "Если ошибка в `new Hero()`, попросите сравнить скобки в вызове и скобки в объявлении конструктора.",
        "Если не работает `this`, временно покажите вариант без `this`, затем верните `this` как более понятную запись для похожих имен.",
        "Если код не запускается, сначала попросите прочитать первую ошибку Visual Studio и показать строку с красным подчеркиванием.",
        "Если ученик потерял проект, помогите через `File` -> `Open` -> `Project/Solution` и попросите открыть файл `.sln`, а не отдельный `Program.cs`.",
    ],
)

doc.add_heading("Дополнительные задания для тех, кто справился раньше", level=3)
add_bullets(
    doc,
    [
        "Добавить третий объект своего класса и вывести его данные.",
        "Добавить метод `Attack()` или `Use()`, который использует одно из полей объекта.",
        "Добавить метод `ShowShortInfo()`, который выводит данные в одну строку.",
        "Изменить значения объектов так, чтобы было видно: конструктор один, а начальные данные у объектов разные.",
        "Подготовить короткое объяснение соседу: чем отличается описание конструктора внутри класса от вызова конструктора через `new`.",
    ],
)

doc.save(OUTPUT)
print(OUTPUT)
