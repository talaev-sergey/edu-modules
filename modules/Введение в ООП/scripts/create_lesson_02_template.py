from docx import Document
from docx.shared import Pt


OUTPUT = "content/lesson_02.docx"


def add_table(document, headers, rows):
    table = document.add_table(rows=1, cols=len(headers))
    table.style = "Table Grid"
    for index, header in enumerate(headers):
        table.rows[0].cells[index].text = header
    for row in rows:
        cells = table.add_row().cells
        for index, value in enumerate(row):
            cells[index].text = value
    return table


def add_label_paragraph(document, label, text=""):
    paragraph = document.add_paragraph()
    paragraph.add_run(label).bold = True
    if text:
        paragraph.add_run(text)
    return paragraph


def add_code(document, code):
    for line in code.strip("\n").splitlines():
        paragraph = document.add_paragraph()
        run = paragraph.add_run(line)
        run.font.name = "Consolas"
        run.font.size = Pt(10)


def add_numbered(document, items):
    for item in items:
        document.add_paragraph(item, style="List Number")


def add_bullets(document, items):
    for item in items:
        document.add_paragraph(item, style="List Bullet")


doc = Document()
doc.add_heading("Урок 2 — Поля и методы: что объект знает и что умеет", level=1)

doc.add_heading("Общая информация", level=2)
add_table(
    doc,
    ["Параметр", "Значение"],
    [
        ["Курс", "Программирование на C#"],
        ["Модуль", "Введение в ООП"],
        ["Номер урока", "2"],
        ["Возраст учащихся", "12-15 лет"],
        ["Продолжительность", "120 мин"],
    ],
)

doc.add_heading("Цель урока", level=2)
doc.add_paragraph(
    "К концу урока ученики смогут добавить в класс поля и методы, создать несколько объектов одного класса "
    "и вызвать методы через точку."
)

doc.add_heading("План урока", level=2)
add_table(
    doc,
    ["Этап", "Время"],
    [
        ["1. Организационный момент", "5 мин"],
        ["2. Теоретическая часть", "25 мин"],
        ["3. Практическая работа", "50 мин"],
        ["4. Самостоятельная работа", "30 мин"],
        ["5. Подведение итогов", "10 мин"],
        ["Итого", "120 мин"],
    ],
)

doc.add_heading("Ход занятия", level=2)

doc.add_heading("1. Организационный момент (5 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Проверить, что у учеников открывается Visual Studio 2022 и виден стартовый экран или прошлый проект.",
        "Коротко повторить прошлый урок: класс — это чертеж, объект — конкретная вещь, `new` создает объект.",
        "Попросить 2-3 учеников привести примеры из домашнего задания: объект, его данные и действия.",
        "Назвать результат урока: сегодня объект будет не просто существовать, а хранить данные и выполнять действия.",
    ],
)

doc.add_heading("2. Теоретическая часть (25 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Объяснять тему через игровую аналогию. Не вводить конструкторы, свойства и `private`: это темы следующих уроков. "
    "На этом занятии важно закрепить базовую связку: поля — что объект знает, методы — что объект умеет делать."
)

doc.add_heading("Поле", level=4)
doc.add_paragraph(
    "Поле — это данные внутри объекта. Например, герой может хранить имя, здоровье и урон. "
    "Если объект — игровой персонаж, то поля отвечают на вопрос: что он знает о себе?"
)
doc.add_paragraph(
    "Пример: у героя есть `Name`, `Health`, `Damage`. У предмета могут быть `Name`, `Price`, `Weight`."
)

doc.add_heading("Метод", level=4)
doc.add_paragraph(
    "Метод — это действие объекта. Если поля говорят, что объект знает, методы говорят, что объект умеет делать. "
    "Герой может показать информацию о себе, атаковать, получить лечение или вывести сообщение в консоль."
)
doc.add_paragraph(
    "На первом уровне достаточно простой формулы: поле хранит значение, метод выполняет команду."
)

doc.add_heading("Точка доступа", level=4)
doc.add_paragraph(
    "Через точку мы обращаемся к конкретному объекту. `hero.Name` означает: взять поле `Name` именно у объекта `hero`. "
    "`hero.Attack()` означает: попросить именно объект `hero` выполнить метод `Attack()`."
)
doc.add_paragraph(
    "Скобки помогают отличить метод от поля: поле обычно читается без скобок, метод вызывается со скобками."
)

doc.add_heading("Записи в блокнот", level=4)
for term, definition in [
    ("Поле", "данные, которые объект хранит о себе."),
    ("Метод", "действие, которое объект умеет выполнять."),
    ("Точка", "способ обратиться к полю или методу конкретного объекта."),
    ("Вызов метода", "запуск действия объекта, например `hero.Attack()`." ),
    ("Несколько объектов", "разные объекты одного класса, у каждого могут быть свои значения полей."),
]:
    paragraph = doc.add_paragraph()
    paragraph.add_run(term).bold = True
    paragraph.add_run(" — " + definition)

doc.add_heading("3. Практическая работа (50 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Вести практику как постепенное снятие опоры. Сначала преподаватель полностью показывает класс `Hero` с полями. "
    "Затем группа вместе добавляет методы `ShowInfo()` и `Attack()`. После этого ученики создают второго героя и проверяют, "
    "что один класс может давать много объектов с разными данными."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Часть 1. Создать проект и класс с полями.")
add_numbered(
    doc,
    [
        "Открыть Visual Studio 2022.",
        "Создать новый проект `Console App` с именем `OopLesson2` или открыть учебную папку проектов.",
        "Открыть файл `Program.cs`.",
        "Добавить класс `Hero` ниже основного кода в `Program.cs`.",
        "Добавить в класс поля `Name`, `Health`, `Damage`.",
        "Создать объект `hero` через `new Hero()`.",
        "Заполнить поля объекта и вывести их через `Console.WriteLine`.",
    ],
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — проект OopLesson2, открыт Program.cs]")
doc.add_paragraph("Код первого шага:")
add_code(
    doc,
    '''
Hero hero = new Hero();
hero.Name = "Рыцарь";
hero.Health = 100;
hero.Damage = 15;

Console.WriteLine("Герой: " + hero.Name);
Console.WriteLine("Здоровье: " + hero.Health);
Console.WriteLine("Урон: " + hero.Damage);

class Hero
{
    public string Name;
    public int Health;
    public int Damage;
}
''',
)
doc.add_paragraph("[СКРИНШОТ: консоль — вывод имени, здоровья и урона героя]")

doc.add_paragraph("Часть 2. Добавить методы.")
add_numbered(
    doc,
    [
        "В классе `Hero` добавить метод `ShowInfo()`.",
        "Перенести вывод имени, здоровья и урона внутрь метода `ShowInfo()`.",
        "Добавить метод `Attack()`.",
        "В основном коде заменить отдельные `Console.WriteLine` на вызовы `hero.ShowInfo()` и `hero.Attack()`.",
        "Запустить проект и проверить вывод в консоли.",
    ],
)
doc.add_paragraph("Код после добавления методов:")
add_code(
    doc,
    '''
Hero hero = new Hero();
hero.Name = "Рыцарь";
hero.Health = 100;
hero.Damage = 15;

hero.ShowInfo();
hero.Attack();

class Hero
{
    public string Name;
    public int Health;
    public int Damage;

    public void ShowInfo()
    {
        Console.WriteLine("Герой: " + Name);
        Console.WriteLine("Здоровье: " + Health);
        Console.WriteLine("Урон: " + Damage);
    }

    public void Attack()
    {
        Console.WriteLine(Name + " атакует и наносит " + Damage + " урона!");
    }
}
''',
)
doc.add_paragraph("[СКРИНШОТ: Visual Studio 2022 — класс Hero с методами ShowInfo() и Attack()]")

doc.add_paragraph("Часть 3. Создать несколько объектов одного класса.")
add_numbered(
    doc,
    [
        "Создать объект `knight` и заполнить его поля.",
        "Создать объект `mage` и заполнить его поля другими значениями.",
        "Вызвать `ShowInfo()` и `Attack()` у каждого объекта.",
        "Сравнить вывод: класс один, а объекты разные.",
    ],
)
doc.add_paragraph("Код для нескольких объектов:")
add_code(
    doc,
    '''
Hero knight = new Hero();
knight.Name = "Рыцарь";
knight.Health = 100;
knight.Damage = 15;

Hero mage = new Hero();
mage.Name = "Маг";
mage.Health = 60;
mage.Damage = 30;

knight.ShowInfo();
knight.Attack();

mage.ShowInfo();
mage.Attack();
''',
)
doc.add_paragraph("[СКРИНШОТ: консоль — вывод двух героев с разными значениями полей]")
add_label_paragraph(
    doc,
    "Ожидаемый результат: ",
    "ученик создал класс `Hero`, добавил поля и методы, создал минимум два объекта одного класса и вызвал методы через точку.",
)

doc.add_heading("4. Самостоятельная работа (30 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
doc.add_paragraph(
    "Первые 5 минут дать ученикам попробовать без готового решения. Если многие застряли, вернуть их к примеру `Hero` и предложить менять только названия. "
    "Через 10 минут провести короткую проверку: у каждого должен быть хотя бы класс, два поля и один объект."
)

doc.add_heading("Задание", level=4)
doc.add_paragraph("Выбери один вариант и создай свой класс по аналогии с `Hero`.")
add_bullets(
    doc,
    [
        "Вариант 1: `Dog` — поля `Name`, `Energy`, `Speed`; методы `ShowInfo()` и `Run()`.",
        "Вариант 2: `Enemy` — поля `Name`, `Health`, `Damage`; методы `ShowInfo()` и `Attack()`.",
        "Вариант 3: `Item` — поля `Name`, `Price`, `Description`; методы `ShowInfo()` и `Use()`.",
    ],
)
doc.add_paragraph("Минимальные требования:")
add_numbered(
    doc,
    [
        "Создать класс.",
        "Добавить минимум три поля.",
        "Добавить минимум два метода.",
        "Создать минимум два объекта одного класса.",
        "Заполнить поля разными значениями.",
        "Вызвать методы у каждого объекта.",
    ],
)

doc.add_heading("Критерии оценки", level=4)
add_table(
    doc,
    ["Результат", "Оценка"],
    [
        ["Класс, 3 поля, 2 метода и 2 объекта созданы; программа запускается, методы вызываются через точку", "Отлично"],
        ["Основная задача выполнена, но есть небольшие ошибки в названиях, выводе или оформлении кода", "Хорошо"],
        ["Есть класс и объект, но методы или второй объект сделаны частично, потребовалась помощь", "Удовлетворительно"],
        ["Программа не запускается или класс с полями и методами не собран", "Требует доработки"],
    ],
)

doc.add_heading("5. Подведение итогов (10 мин)", level=3)
add_label_paragraph(doc, "Действия преподавателя:")
add_bullets(
    doc,
    [
        "Попросить нескольких учеников показать свой класс и назвать, где поля, а где методы.",
        "Спросить группу: почему `Health` — поле, а `Attack()` — метод?",
        "Закрепить идею: один класс может создавать много объектов, и каждый объект хранит свои значения.",
        "Анонсировать следующий урок: конструктор поможет создавать объект сразу с нужными данными, без длинного ручного заполнения полей.",
    ],
)
add_label_paragraph(doc, "Вопросы для рефлексии:")
add_bullets(
    doc,
    [
        "Что хранит поле?",
        "Что делает метод?",
        "Почему метод вызывается со скобками?",
        "Что означает точка в записи `hero.Attack()`?",
        "Чем отличаются `knight` и `mage`, если они созданы по одному классу `Hero`?",
    ],
)

doc.add_heading("Домашнее задание", level=2)
doc.add_paragraph("Вариант без компьютера:")
add_numbered(
    doc,
    [
        "Придумать игровой объект: `Dog`, `Robot`, `Enemy`, `Door` или свой вариант.",
        "Записать для него 3 поля.",
        "Записать для него 3 метода.",
        "Оформить пример словами: `Класс Dog. Поля: Name, Energy, Speed. Методы: Bark(), Run(), Sleep()`.",
    ],
)
doc.add_paragraph("Вариант с компьютером:")
add_numbered(
    doc,
    [
        "Открыть проект `OopLesson2`.",
        "Создать свой класс с 3 полями и 2 методами.",
        "Создать два объекта этого класса с разными значениями.",
        "Запустить программу и проверить вывод.",
    ],
)

doc.add_heading("Методические заметки преподавателя", level=2)
doc.add_heading("Возможные сложности", level=3)
add_bullets(
    doc,
    [
        "Ученики могут путать поле и метод: если это данные, это поле; если действие, это метод.",
        "Частая ошибка: забытые скобки при вызове метода, например `hero.Attack` вместо `hero.Attack()`.",
        "Частая ошибка: разные имена переменных, например создали `Hero hero`, а вызывают `knight.ShowInfo()`.",
        "Ученики могут забывать точку с запятой после строк кода.",
        "Ученики могут поставить класс внутрь другого блока фигурных скобок. Проверяйте расположение `class Hero`.",
        "Слабые навыки Windows проявятся в том, что ученик не найдет свой проект или откроет не тот файл. Просите показать `Solution Explorer` и путь к проекту.",
    ],
)

doc.add_heading("Способы помощи", level=3)
add_bullets(
    doc,
    [
        "Если ученик не понимает поле, спросите: какие данные герой должен помнить о себе?",
        "Если ученик не понимает метод, спросите: какое действие должен выполнить объект?",
        "Если ученик забыл скобки, напомните: метод — это действие, действие запускается скобками.",
        "Если ученик не может создать второй объект, попросите скопировать блок создания первого объекта и поменять имя переменной и значения полей.",
        "Если ученик просит готовый код, дайте только следующий ориентир: найди пример `Hero` и замени тему на свою.",
        "Если код не запускается, сначала попросите прочитать первую ошибку Visual Studio и показать строку с красным подчеркиванием.",
    ],
)

doc.add_heading("Дополнительные задания для тех, кто справился раньше", level=3)
add_bullets(
    doc,
    [
        "Добавить третий объект того же класса с другими значениями.",
        "Добавить метод, который использует сразу два поля, например имя и урон.",
        "Добавить метод `Heal()` или `Rest()`, который пока только выводит сообщение в консоль.",
        "Подготовить короткое объяснение для соседа: почему один класс может создавать много разных объектов.",
    ],
)

doc.save(OUTPUT)
print(OUTPUT)
